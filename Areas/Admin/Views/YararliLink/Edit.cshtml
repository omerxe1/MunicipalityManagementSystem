@model kocaaliv2.Models.YararliLink
@{
    ViewData["Title"] = "Yararlı Link Düzenle";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h4><i class="fas fa-edit me-2"></i>Yararlı Link Düzenle</h4>
    <a href="@Url.Action("Index", "YararliLink", new { area = "Admin" })" class="btn btn-secondary">
        <i class="fas fa-arrow-left me-2"></i>Geri Dön
    </a>
</div>

<div class="card">
    <div class="card-body">
        <form asp-action="Edit" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Id" />
            
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="mb-3">
                <label asp-for="LinkAdi" class="form-label">Link Adı <span class="text-danger">*</span></label>
                <input asp-for="LinkAdi" class="form-control" />
                <span asp-validation-for="LinkAdi" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Url" class="form-label">URL <span class="text-danger">*</span></label>
                <input asp-for="Url" class="form-control" placeholder="https://..." />
                <span asp-validation-for="Url" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Kategori" class="form-label">Kategori <span class="text-danger">*</span></label>
                <select asp-for="Kategori" class="form-control">
                    <option value="">Seçiniz</option>
                    <option value="Ulusal">Ulusal</option>
                    <option value="Yerel">Yerel</option>
                </select>
                <span asp-validation-for="Kategori" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="LogoUrl" class="form-label">Logo URL</label>
                <input asp-for="LogoUrl" class="form-control" placeholder="https://..." />
                <span asp-validation-for="LogoUrl" class="text-danger"></span>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label asp-for="SiraNo" class="form-label">Sıra No</label>
                    <input asp-for="SiraNo" type="number" class="form-control" />
                    <span asp-validation-for="SiraNo" class="text-danger"></span>
                </div>
                <div class="col-md-6 mb-3">
                    <div class="form-check mt-4">
                        <input asp-for="AktifMi" class="form-check-input" type="checkbox" />
                        <label asp-for="AktifMi" class="form-check-label">Aktif</label>
                    </div>
                </div>
            </div>

            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-save me-2"></i>Güncelle
                </button>
                <a href="@Url.Action("Index", "YararliLink", new { area = "Admin" })" class="btn btn-secondary">
                    <i class="fas fa-times me-2"></i>İptal
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}






